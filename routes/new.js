const storedPassword = "password123";  // Stored plain text password
const enteredPassword = "password123"; // User entered password

if (enteredPassword === storedPassword) {
    console.log("✅ Match: true");
} else {
    console.log("❌ Match: false");
}
