{
  "builds": [
    {
      "src": "/api/app.py",
      "use": "@vercel/python",
      "config": { "runtimeVersion": "3.10" }
    }
  ],
  "scripts": {
    "vercel-build": "apt-get update && apt-get install -y cmake && pip install -r requirements.txt"
  }
}
